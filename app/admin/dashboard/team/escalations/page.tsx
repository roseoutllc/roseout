import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { AdminReviewListPage } from "@/components/WorkspaceListPage";
export const dynamic="force-dynamic"; export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamManagement); const {data=[]}=await supabaseAdmin.from("workspace_escalations").select("*").order("created_at",{ascending:false}).limit(100); return <AdminReviewListPage title="Escalations" description="Workspace escalations for owner anger, wrong business info, claim issues, reservations, billing, legal/privacy, repeated failed outreach, do-not-contact, and other risks." rows={data||[]} table="workspace_escalations"/> }
