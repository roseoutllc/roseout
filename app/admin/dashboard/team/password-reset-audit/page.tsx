import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { AdminReviewListPage } from "@/components/WorkspaceListPage";
export const dynamic="force-dynamic"; export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamSecurityAudit); const {data=[]}=await supabaseAdmin.from("password_reset_audit_logs").select("*").order("created_at",{ascending:false}).limit(100); return <AdminReviewListPage title="Password Reset Audit" description="Every owner password-reset assistance attempt is logged with masked target email, requester, reason, support context, and provider response." rows={data||[]} table="password_reset_audit_logs"/> }
