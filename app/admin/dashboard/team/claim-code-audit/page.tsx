import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { AdminReviewListPage } from "@/components/WorkspaceListPage";
export const dynamic="force-dynamic"; export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamSecurityAudit); const {data=[]}=await supabaseAdmin.from("claim_code_audit_logs").select("*").order("created_at",{ascending:false}).limit(100); return <AdminReviewListPage title="Claim Code Audit" description="Audited claim-code sends and logged deliveries by workspace users, with channels, platforms, masked targets, and rate-limit metadata." rows={data||[]} table="claim_code_audit_logs"/> }
