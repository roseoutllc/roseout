import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { AdminReviewListPage } from "@/components/WorkspaceListPage";
export const dynamic="force-dynamic";
export default async function Review(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamManagement); const [{data:sessions=[]},{data:changes=[]},{data:proofs=[]},{data:visits=[]},{data:social=[]}]=await Promise.all([supabaseAdmin.from("team_work_sessions").select("id,work_type,status,approval_status,user_notes,created_at").eq("approval_status","pending_review").limit(50),supabaseAdmin.from("location_change_requests").select("*").eq("status","pending_review").limit(50),supabaseAdmin.from("team_proofs").select("*").eq("manager_review_status","pending_review").limit(50),supabaseAdmin.from("ambassador_site_visits").select("*").eq("manager_review_status","pending_review").limit(50),supabaseAdmin.from("ambassador_social_outreach").select("*").eq("manager_review_status","pending_review").limit(50)]); const rows=[...(sessions||[]).map((r:any)=>({...r,title:`Work session: ${r.work_type}`, status:r.approval_status, table:"team_work_sessions"})),...(changes||[]),...(proofs||[]),...(visits||[]),...(social||[])]; return <AdminReviewListPage title="Manager Review" description="Unified review queue for sessions, site visits, social proof, protected field changes, and proof uploads. Work sessions use their dedicated Work Sessions review buttons." rows={rows} table="location_change_requests"/> }
