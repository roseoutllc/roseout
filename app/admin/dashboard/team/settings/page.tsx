import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";
export const dynamic="force-dynamic"; export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamManagement); return <main className="px-4 py-6 text-white"><div className="mx-auto max-w-4xl rounded-3xl border border-white/10 bg-[#111] p-6"><h1 className="text-3xl font-black">Team Tools Settings</h1><p className="mt-3 text-sm font-bold leading-6 text-white/55">Workspace behavior is controlled by Supabase team_member_profiles permissions, allowed_work_types, do-not-contact fields, Edge Function config, and manager review routes. Use Team Members to update per-user settings.</p></div></main> }
