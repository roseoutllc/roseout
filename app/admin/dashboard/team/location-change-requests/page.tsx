import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { AdminReviewListPage } from "@/components/WorkspaceListPage";
export const dynamic="force-dynamic"; export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.teamManagement); const {data=[]}=await supabaseAdmin.from("location_change_requests").select("*").order("created_at",{ascending:false}).limit(100); return <AdminReviewListPage title="Location Change Requests" description="Protected workspace CRM field changes. Approving stores manager review status and audit logs; protected fields cannot be edited directly by team members." rows={data||[]} table="location_change_requests"/> }
